import React from 'react';

// TODO: show profile settings at top, and family details below (editable if head of family -> get invite link here as well?)
// TODO: figure out if there's a map package that users can find their home address so we can get the lat/long from it to store in the family doc

const Profile = () => {
  return (
    <div>
        Profile page
    </div>
  );
}

export default Profile;
