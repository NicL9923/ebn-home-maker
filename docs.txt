***Data Structs***
Profiles (ID: user.uid)
    budgetId
    familyId
    firstName
    imgLink

Families
    headOfFamily
    location
        lat
        long
    members
    name
    openweathermap_api_key
    gmaps_api_key
    gcal_id
    pets
        imgLink
        name
    boardMarkdown
    vehicles
    residences

Budgets
    name
    id (same as docID)
    monthlyNetIncome
    categories
        name
        subcategories
            name
            currentSpent
            totalAllotted
    editors
    savingsBlobs
        currentAmt
        goalAmt
        name
    transactions
        amt
        category
        name
        timestamp
    investmentAccts
        name
        broker
        curValue
        prevValues
            monthYear
            value

Residences
    id
    img
    name
    yearBuilt
    yearPurchased
    maintenanceMarkers
        houseAgeYears
        maintenanceReq

Vehicles
    id
	img
    year
	make
	model
	trim
	engine
    vin
    licensePlate
	miles
	serviceLogEntries
		date
		note
	maintenanceMarkers
		mileage
		maintenanceReq

***Plans/TODOs***
General TODOs-----------------------------
-Input validation (Simple regex should cover most or all cases)
    -Handle forms better (don't allow submit with validation errors, displaying input/validation errors + required fields, etc.)
-Fix dialog maxWidths

-Button (on /profile) to back-up data (mass dump of: budget, family info, maintenance items, etc)
-Replace all merge-true setDoc calls with updateDoc *derp*
-Handle recurring events for /info


Finance TODOs-----------------------------
-Edit investment account value points (incl. current)
    -Appropriate spacing between date points
-Ability to edit existing transactions (https://mui.com/x/react-data-grid/editing/)

-FS Functions -> generate report at end of month with different data (budget
data - income, allotted, spent; transactions; savings/investment account
checkpoints), clear budget.transactions     ***Spreadsheet or just PDF?...both???

-Track assets & loans (mortgage, auto loans, etc.)

Maintenance TODOs------------------------  ***PRIORITY***
-Implement editing
-Log management
-Adding/displaying maintenance markers

-Probably a good idea to just show simple list of items on main page, then
click an item to pull up a new page with just that item

Profile TODOs------------------------------
-Add pets, remove members
-Edit family data


Ideas-----------------------
-Refactor to TypeScript if insane
-Add secondary editors in addition to head of household?

-Implement localization *VERY low priority*
-Light/dark mode? *VERY low priority*