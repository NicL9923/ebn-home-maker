***Data Structs***
Profiles (ID: user.uid)
    budgetId
    familyId
    firstName
    imgLink

Families
    headOfFamily
    location
        lat
        long
    members
    name
    openweathermap_api_key
    gmaps_api_key
    pets
        imgLink
        name
    events
        title
        start: Date
        end: Date
        repeatFrequency?
        allDay?
    boardMarkdown
    vehicles
    residences

Budgets
    id (same as docID)
    monthlyNetIncome
    categories
        name
        subcategories
            name
            currentSpent
            totalAllotted
    editors
    savingsBlobs
        currentAmt
        goalAmt
        name
    transactions
        amt
        category
        name
        timestamp
    investmentAccts
        name
        broker
        curValue
        prevValues
            monthYear
            value

Residences
    id
    img
    name
    yearBuilt
    yearPurchased
    maintenanceMarkers
        houseAgeYears
        maintenanceReq

Vehicles
    id
	img
    year
	make
	model
	trim
	engine
    vin
    licensePlate
	miles
	serviceLogEntries
		date
		note
	maintenanceMarkers
		mileage
		maintenanceReq

***Plans/TODOs***
General TODOs-----------------------------
-Figure out how to reduce package size...likely culprits are recharts and ...? -> see if any way to use only specific packages needed

-Add spinners when stuff is being fetched/loaded
-Input validation (Simple regex should cover most or all cases)
    -Handle forms better (don't allow submit with validation errors, etc.)
-Fix dialog maxWidths (?)
-'Are you sure?' dialogs for deleting stuff

-Light/dark mode? *VERY low priority*

Finance TODOs-----------------------------
-Edit investment account value points (incl. current)
-Ability to edit existing transactions (https://mui.com/x/react-data-grid/editing/)

-FS Functions -> generate report at end of month with different data (budget
data - income, allotted, spent; transactions; savings/investment account
checkpoints), clear budget.transactions

Maintenance TODOs------------------------  ***PRIORITY***
-Implement editing
-Log management
-Adding/displaying maintenance markers

-Integrate some level of stuff like KBB trade values, etc?

Information TODOs-------------------------  ***PRIORITY***
-Upcoming events
-Family calendar (manage/CRUD events here too)
    -Adding/removing/editing events

Ideas-----------------------
-Track loans (mortgage, auto loans, etc.)?
-Make separate dialog components?
    -Are you sure? (title, text, callbackCancel, callbackYes)
    -Setting a value (title, inputLabel, regexValidation, callbackCancel, callbackSave)

-Transfer to TypeScript if insane
-Add secondary editors in addition to head of household?

-Implement localization